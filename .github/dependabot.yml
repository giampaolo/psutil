version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    # Bundle all action bumps into one PR instead of one each.
    groups:
      github-actions:
        patterns:
          - "*"
